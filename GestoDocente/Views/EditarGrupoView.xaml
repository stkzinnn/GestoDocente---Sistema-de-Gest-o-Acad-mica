<Window x:Class="GestoDocente.Views.EditarGrupoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Grupo"
        Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F7">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nome do Grupo:"
                   FontWeight="Bold"
                   FontSize="14"
                   Foreground="#2C3E50"
                   Grid.Row="0"
                   Margin="0,0,0,5"/>

        <TextBox x:Name="NomeTextBox"
                 Grid.Row="1"
                 Height="30"
                 Padding="5"
                 Background="White"
                 BorderBrush="#B2BABB"
                 BorderThickness="1"
                 FontSize="14"/>

        <Button Content="💾 Salvar"
                Grid.Row="2"
                Width="100"
                Height="35"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"
                Padding="5"
                FontWeight="Bold"
                Background="#D6EAF8"
                Foreground="#154360"
                BorderBrush="#AED6F1"
                Click="Salvar_Click"/>
    </Grid>
</Window>
