<Window x:Class="GestoDocente.Views.AdicionarAlunoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adicionar Aluno"
        Height="370" Width="480"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA"
        AllowsTransparency="False"
        WindowStyle="SingleBorderWindow"
        Opacity="0"
          SizeToContent="Height"
        MinWidth="480"
        MaxWidth="600"
        ResizeMode="NoResize" >

<!-- Fade-in -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.4"
                                     AccelerationRatio="0.2" DecelerationRatio="0.8"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border Background="White" CornerRadius="10" Padding="20" BorderBrush="#DDE2E6" BorderThickness="1">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Título -->
            <TextBlock Text="➕ Novo Aluno"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- Formulário -->
            <StackPanel Grid.Row="1" Margin="20,0,20,10">
                <TextBlock Text="Número do Aluno" Style="{StaticResource FormLabel}"/>
                <TextBox Text="{Binding Numero}" Style="{StaticResource RoundedTextBox}"/>

                <TextBlock Text="Nome Completo" Style="{StaticResource FormLabel}"/>
                <TextBox Text="{Binding Nome}" Style="{StaticResource RoundedTextBox}"/>

                <TextBlock Text="Email Institucional" Style="{StaticResource FormLabel}"/>
                <TextBox Text="{Binding Email}" Style="{StaticResource RoundedTextBox}"/>
            </StackPanel>

            <!-- Botões -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="💾 Guardar"
                        Command="{Binding GuardarCommand}"
                        Style="{StaticResource ModernButton}" Width="110" Margin="5"/>

                <Button Content="❌ Cancelar"
                        Command="{Binding CancelarCommand}"
                        Style="{StaticResource CancelButton}" Width="110" Margin="5"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>